#include <stdio.h>
/**
*main - Etry point
*
*Description: print alphabets in lowercase followed
*by a newline
*
*Return: return 0 (Success)
*/
int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++0
{
if (ch != 'q' && ch != 'e')
{
putchar(ch);
}
else
{
}
}
putchar('\n');
return (0);
}
